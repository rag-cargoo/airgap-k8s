# Manual kubeadm

- Kubernetes 공식 `kubeadm` 절차를 사람이 직접 실행하는 기준 파일과 명령을 둔다.
- 온라인 다운로드 단계는 `assets/offline-assets/kubernetes/` 자산 사용으로 치환한다.
- 공통 사전점검과 반입 경로 기준은 `../common/`을 따른다.
- bastion 경유든 폐쇄망 내부 직접 작업이든, 수동 절차는 대상 노드에 접속한 뒤 같은 명령을 실행하는 방식으로 유지한다.
